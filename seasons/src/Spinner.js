import React from "react";

const Spinner = () => {
  return (
    <div className="ui active dimmer">
      <div className="ui border-right text loader">Loading...</div>
    </div>
  );
};

export default Spinner;
